==About
Helm - a greylist protocol java server

==Build
Make sure you have Java and Maven2 installed.

Call 'mvn package' to build a release zip file
 target/helm-<version>-bin.zip

==Use
java -jar helm.jar conf.param create-database
java -jar helm.jar conf.param start &
java -jar helm.jar conf.param stop

See https://roundup.it.su.se/confluence/display/helm/ for more options.
